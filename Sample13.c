#include <stdlib.h>
#include <stdio.h>

typedef struct LNode
{
    int data;
    struct LNode *next;

} LNode, *LinkList;

int main()
{
    // LNode *p = (LNode *)malloc(sizeof(LNode));
    // LNode p = (LNode)malloc(sizeof(LNode));
    // printf("%d\n", p.data);
    return 0;
}